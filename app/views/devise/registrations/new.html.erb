<h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900" >
  Registrate
</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), class: 'mt-8 spece-y-6') do |f| %>
  
  <div class="rounded-md- shadow-sm -space-y-px">
    <div>
      <%= f.label :email, "Correo Electronico", class: 'sr-only' %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "email", class: 'appearance-none rounded-node relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-t-md focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm' %>
    </div>

    <div>
      <%= f.label :password, "Contraseña", class: 'sr-only' %>
      <%= f.password_field :password, autocomplete: "new-password", placeholder: "Password", class: 'appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm' %><br />
    <%= f.label :password_confirmation, "Confirmar Contraseña", class: 'sr-only' %>
    <%= f.password_field :password, autocomplete: "new-password", placeholder: "Confirmar Contraseña", class: 'appaerance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-t-md focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm' %><br />
    </div>
  </div>

  <div>
    <%= f.submit "Registrate🤑", class: 'group relative w-fullflex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 cursor-pointer' %>
  </div>

<% end %>
